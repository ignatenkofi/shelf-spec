[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openshelf"
dynamic = ["version"]
description = "shelf-spec: portable, vendor-neutral agent memory as a git repo of Markdown — spec, validator, thin MCP server and CLI."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Filipp Ignatenko", email = "ignatenkofi@gmail.com" }
]
keywords = ["mcp", "model-context-protocol", "agent-memory", "markdown", "git", "spec", "llm"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Processing :: Markup :: Markdown",
]

dependencies = [
    "mcp>=1.2.0",
    "pydantic>=2.6",
    "pyyaml>=6.0",
    "jsonschema>=4.21",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.5.0",
]

[project.scripts]
openshelf = "openshelf.cli:main"

[project.urls]
Homepage = "https://github.com/ignatenkofi/openshelf"
Repository = "https://github.com/ignatenkofi/openshelf"

[tool.hatch.version]
path = "src/openshelf/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/openshelf"]

[tool.hatch.build.targets.wheel.force-include]
# The canonical schema lives in spec/ (single source of truth); ship a copy
# inside the wheel so the installed package can validate manifests offline.
"spec/shelf.schema.json" = "openshelf/spec/shelf.schema.json"

[tool.hatch.build.targets.sdist]
include = [
    "src/",
    "tests/",
    "spec/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "pyproject.toml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "UP", # pyupgrade
]
ignore = ["E501"]  # line too long — handled by formatter

[tool.ruff.lint.isort]
known-first-party = ["openshelf"]
