# shelf.yml — openshelf manifest (shelf-spec v0)
# Adding this one file makes the existing shelf spec-conformant (mode:
# single); nothing else migrates. .docshelf.json stays as the
# implementation config of docshelf-mcp.
spec_version: "0.1"
mode: single
name: "SQST — working memory (memshelf M0)"
profile: memory
docs_root: docs
categories:
  - topics
  - research
  - sessions
index:
  path: INDEX.md
  generated_by: docshelf-mcp
ledger:
  path: ledger.tsv
policy:
  path: POLICY.md
