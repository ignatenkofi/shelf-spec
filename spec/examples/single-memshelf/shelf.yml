# Example: memory shelf (memshelf profile), single mode.
# Matches the layout of a live memory shelf: episode categories,
# ledger, policy, index generated by the reference implementation.
spec_version: "0.1"
mode: single
name: "Example working memory shelf"
profile: memory
docs_root: docs
categories:
  - topics
  - research
  - sessions
index:
  path: INDEX.md
  generated_by: docshelf-mcp
ledger:
  path: ledger.tsv
policy:
  path: POLICY.md
