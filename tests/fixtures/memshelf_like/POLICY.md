# POLICY — fixture shelf

Synthetic policy for tests. No real personal identifiers; synthetic names
only (e.g. "Testova", "Primerov"). Credential-shaped strings become
`redacted:<kind>` before hitting disk. Raw transcripts are never committed.
